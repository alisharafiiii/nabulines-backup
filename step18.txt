// STEP 18: TERMINAL-STYLE TYPING ANIMATION

// Current state of the UI:
// 1. NABULINES logo at the top
// 2. NABULINES text below the logo
// 3. Terminal-style typing animation with 4 rotating messages
// 4. Connect buttons below

// Terminal animation settings in app/page.tsx:
import { TypeAnimation } from "react-type-animation";

// Typing animation component with these properties:
// - White text color with neon green cursor
// - 120ms typing speed, 80ms deletion speed
// - 5 second display time for each message
// - 500ms pause between messages
// - 4 rotating messages
// - Custom div-based cursor with glow effect

<div className={`h-8 ${pressStart.className} relative flex items-center`}>
  <TypeAnimation
    sequence={[
      'welcome to The System.',
      5000, // Pause for 5 seconds before deleting
      '',
      500,
      'this isn\'t for everyone.',
      5000,
      '',
      500,
      'access isn\'t given. it\'s earned.',
      5000,
      '',
      500,
      'are you one of the real ones?',
      5000,
      '',
      500,
      'welcome to The System.',
      5000
    ]}
    wrapper="p"
    speed={{ type: 'keyStrokeDelayInMs', value: 120 }}
    deletionSpeed={{ type: 'keyStrokeDelayInMs', value: 80 }}
    style={{ 
      fontSize: '0.8rem', 
      display: 'inline-block', 
      color: '#FFFFFF' 
    }}
    repeat={Infinity}
    cursor={false}
    omitDeletionAnimation={false}
    className="text-white"
  />
  <span className="inline-block ml-[1px] w-[7px] h-[14px] bg-[#00FF00] opacity-80 animate-pulse shadow-[0_0_5px_#00FF00,0_0_10px_#00FF00]"></span>
</div>

// Notes:
// - Custom cursor has neon glow effect with shadow-[0_0_5px_#00FF00,0_0_10px_#00FF00]
// - Cursor is animated with animate-pulse
// - The cursor and text are aligned with flex items-center
// - Messages rotate in sequence with 5 second display time 